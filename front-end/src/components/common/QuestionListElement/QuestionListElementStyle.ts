import styled from "styled-components";

import StyledButton from "../Button/ButtonStyle";

const StyledQuestion = styled(StyledButton)`
  width: 100%;
  text-align: left;

  p {
    margin: 0;
  }
`;

export default StyledQuestion;
