import styled from "styled-components";
import { centeredColumn } from "../../styles/values";

const FinalScoreContainer = styled.main`
  ${centeredColumn};

  p {
    margin-top: 2rem;
  }
`;

export default FinalScoreContainer;